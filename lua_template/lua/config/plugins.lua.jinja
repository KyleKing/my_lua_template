{% if project_type == 'nvim-config' -%}
-- Plugin declarations.
--
-- No plugin manager is pinned by the template. Bootstrap the one you want here
-- (lazy.nvim, mini.deps, paq) and declare plugins below; init.lua already
-- requires this module.

return {}
{% endif -%}
