{% if project_type == 'nvim-config' -%}
-- Namespace for this configuration's own modules.
--
-- Core settings live in lua/config/. Add feature modules alongside them and
-- require them from init.lua.
{% endif -%}
